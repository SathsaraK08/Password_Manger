# Environment variables for the password vault backend

# JWT secret used to sign and verify JWT tokens.
SECRET_KEY=your-secret-key-here

# The JWT signing algorithm.
ALGORITHM=HS256

# How long access tokens are valid in minutes (e.g. 60 for one hour).
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Database connection string. Use SQLite by default for development.
# Example for SQLite: sqlite:///./vault.db
# Example for PostgreSQL: postgresql+psycopg2://user:password@localhost/dbname
DATABASE_URL=sqlite:///./vault.db

# 32‑byte key for AES‑256 encryption encoded in base64. Generate a random 32 byte key and base64 encode it.
# Never commit your real AES key into version control.
AES_KEY=base64_encoded_32byte_key_here